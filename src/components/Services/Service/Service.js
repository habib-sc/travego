import React from 'react';

const Service = (props) => {
    const {name, img, price, text} = props.service;
    return (
        <div>
            Service
        </div>
    );
};

export default Service;